basePath: /api/v1
definitions:
  github_com_ahmetcanc_notify-one_internal_domain.Notification:
    properties:
      batch_id:
        type: string
      channel:
        $ref: '#/definitions/github_com_ahmetcanc_notify-one_internal_domain.NotificationChannel'
      content:
        type: string
      created_at:
        type: string
      id:
        type: string
      idempotency_key:
        type: string
      next_retry_at:
        type: string
      priority:
        $ref: '#/definitions/github_com_ahmetcanc_notify-one_internal_domain.NotificationPriority'
      recipient:
        type: string
      retry_count:
        type: integer
      scheduled_at:
        type: string
      status:
        $ref: '#/definitions/github_com_ahmetcanc_notify-one_internal_domain.NotificationStatus'
      updated_at:
        type: string
    type: object
  github_com_ahmetcanc_notify-one_internal_domain.NotificationChannel:
    enum:
    - sms
    - email
    - push
    type: string
    x-enum-varnames:
    - ChannelSMS
    - ChannelEmail
    - ChannelPush
  github_com_ahmetcanc_notify-one_internal_domain.NotificationPriority:
    enum:
    - low
    - normal
    - high
    type: string
    x-enum-varnames:
    - PriorityLow
    - PriorityNormal
    - PriorityHigh
  github_com_ahmetcanc_notify-one_internal_domain.NotificationStatus:
    enum:
    - pending
    - processing
    - sent
    - failed
    - cancelled
    type: string
    x-enum-varnames:
    - StatusPending
    - StatusProcessing
    - StatusSent
    - StatusFailed
    - StatusCancelled
host: localhost:3333
info:
  contact:
    name: Ahmet Can Ceylan
  description: High-performance, scalable notification service.
  title: NotifyOne API
  version: "1.0"
paths:
  /metrics:
    get:
      description: Get current status of queues and system health.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: System Metrics
      tags:
      - Observability
  /notifications:
    get:
      description: Retrieves notifications with filtering and pagination.
      parameters:
      - description: Filter by status
        in: query
        name: status
        type: string
      - description: Filter by channel
        in: query
        name: channel
        type: string
      - default: 20
        description: Limit for pagination
        in: query
        name: limit
        type: integer
      - description: Offset for pagination
        in: query
        name: offset
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/github_com_ahmetcanc_notify-one_internal_domain.Notification'
            type: array
      summary: List notifications
      tags:
      - Notifications
    post:
      consumes:
      - application/json
      description: Creates a new notification and sends it.
      parameters:
      - description: Notification details
        in: body
        name: notification
        required: true
        schema:
          $ref: '#/definitions/github_com_ahmetcanc_notify-one_internal_domain.Notification'
      produces:
      - application/json
      responses:
        "202":
          description: Returns notification ID and status
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Send single notification
      tags:
      - Notifications
  /notifications/batch:
    post:
      consumes:
      - application/json
      description: Creates multiple notifications in a single request.
      parameters:
      - description: List of notifications
        in: body
        name: notifications
        required: true
        schema:
          items:
            $ref: '#/definitions/github_com_ahmetcanc_notify-one_internal_domain.Notification'
          type: array
      produces:
      - application/json
      responses:
        "202":
          description: Accepted
          schema:
            additionalProperties: true
            type: object
      summary: Send batch notifications
      tags:
      - Notifications
  /notifications/batch/{batchId}/cancel:
    patch:
      description: Cancels all pending notifications in a batch.
      parameters:
      - description: Batch ID
        in: path
        name: batchId
        required: true
        type: string
      responses:
        "204":
          description: No Content
      summary: Cancel batch
      tags:
      - Notifications
swagger: "2.0"
